browser
=======

Package browser implements functions for opening a web browser

To install browser run the following command:

```
    go get gopkgs.com/browser.v1
```

Then use the following import path to ensure a stable API:

```go
    import "gopkgs.com/browser.v1
```

For documentation and available versions,
see http://gopkgs.com/browser.
